# ADN IV EV Implementation Plan

> Execute in-repo with TDD. No commit unless asked.

**Goal:** Hatch DNA, 6 IVs/EVs, Status page 4 with helix, NVS schema 3.

**Architecture:** `PetState` owns genome; `SettingsStore` persists it; `Panels` draws page 3; `App` rolls entropy on hatch.

**Tech Stack:** C++ vpet_core, Unity native tests, T-Display firmware.
